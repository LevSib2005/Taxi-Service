
CREATE TABLE drivers (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   name VARCHAR(255),
   email VARCHAR(255),
   phone VARCHAR(255),
   license_number VARCHAR(255),
   status VARCHAR(255),
   created_at TIMESTAMP WITHOUT TIME ZONE,
   CONSTRAINT pk_drivers PRIMARY KEY (id)
);

CREATE TABLE passengers (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   name VARCHAR(255),
   email VARCHAR(255),
   phone VARCHAR(255),
   created_at TIMESTAMP WITHOUT TIME ZONE,
   CONSTRAINT pk_passengers PRIMARY KEY (id)
);

CREATE TABLE refresh_token (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   token_hash VARCHAR(255),
   user_id BIGINT,
   user_type VARCHAR(255),
   expires_at TIMESTAMP WITHOUT TIME ZONE,
   created_at TIMESTAMP WITHOUT TIME ZONE,
   CONSTRAINT pk_refreshtoken PRIMARY KEY (id)
);

ALTER TABLE drivers ADD CONSTRAINT UC_DRIVERS_EMAIL UNIQUE (email);

ALTER TABLE passengers ADD CONSTRAINT UC_PASSENGERS_EMAIL UNIQUE (email);

ALTER TABLE refresh_token ADD CONSTRAINT UC_REFRESHTOKEN_TOKENHASH UNIQUE (token_hash);