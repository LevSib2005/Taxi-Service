server:
  port: 8000
  forward-headers-strategy: framework

spring:
  application:
    name: gateway-service

  cloud:
    gateway:
      server:
        webflux:
          routes:
            # User Service
            - id: user-service
              uri: http://localhost:8080
              predicates:
                - Path=/passengers/**, /drivers/**, /auth/**

            # Trip Service
            - id: trip-service
              uri: http://localhost:8082
              predicates:
                - Path=/trips/**

            # User Service - api-docs
            - id: user-service-api-docs
              uri: http://localhost:8080
              predicates:
                - Path=/user-service/v3/api-docs/**
              filters:
                - RewritePath=/user-service/v3/api-docs(?<path>.*), /api-docs$\{path}

            # Trip Service - api-docs
            - id: trip-service-api-docs
              uri: http://localhost:8082
              predicates:
                - Path=/trip-service/v3/api-docs/**
              filters:
                - RewritePath=/trip-service/v3/api-docs(?<path>.*), /api-docs$\{path}

gateway:
  secret: ${JWT_SECRET:dev-secret-key-min-32-chars-for-hmac-sha256}
  issuer: ${JWT_ISSUER:user-service}
  header: ${JWT_HEADER:X-Gateway-Header}
  header-key: ${HEADER_KEY:dev-secret-key-for-microservices}
  open-routes:
    - /actuator/
    - /swagger-ui/
    - /swagger-ui.html
    - /user-service/v3/api-docs
    - /trip-service/v3/api-docs
    - /webjars/
    - /v3/api-docs/

# Swagger UI в Gateway
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    # ✅ Настройка для авторизации
    urls:
      - name: user-service
        url: /user-service/v3/api-docs
      - name: trip-service
        url: /trip-service/v3/api-docs
    # ✅ Показываем поле для ввода токена
    persistent-auth: true
    display-request-duration: true

logging:
  level:
    com.example.gateway_service: DEBUG
    org.springframework.cloud.gateway: INFO